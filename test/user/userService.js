const { assert } = require('chai'),
    request = require('request');

describe('userService', () => {
    beforeEach(async () => {
        // await start server
        // Insert some users
    });
    it('should run root endpoint', () => {
    });
});
