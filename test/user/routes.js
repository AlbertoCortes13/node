const { expect, assert } = require('chai'),
    request = require('request');

describe('User routes', () => {
    beforeEach(async () => {
        // await start server
        // Insert some users
    });
    it('should run root endpoint', async () => {
        // let response = await request(`${process.env.host}:${process.env.port}/users/`);
        // expect(response).to.be.a('json');
    });
});
